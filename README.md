# STT perf PR — UI evidence

Screenshots and the walkthrough recording for PR #12444.

Kept on this branch rather than in the PR because `.gitignore` records
`temp-screenshots/` as never-committed, and a review branch should not carry
binaries. A stable branch also means the PR body's URLs survive a force-push.
